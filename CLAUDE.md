# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Python project for integrating Claude with Jupyter notebooks. The repository is in early development stages.

## Repository Structure

- `claude_workbench/` - Working directory for Claude-Jupyter integration (currently empty)
- `.gitignore` - Standard Python/Jupyter ignore patterns including `.ipynb_checkpoints`

## Development Environment

This project uses Python and Jupyter notebooks. Virtual environments are gitignored (`.venv`, `venv/`, `env/`).
